# Алгоритмы на стеках и очередях

В данном разделе рассматриваются классические задачи для получения практических навыков работы со стеком и очередью. Реализованы два фундаментальных алгоритма, широко используемых в компьютерных науках.

## 📋 Содержание

1. [PSP - Правильная скобочная последовательность]([#psp](https://github.com/Haise3s/Grokking_algorithms/blob/main/Stack%20and%20queue/CPS.py))
2. [RPN - Обратная польская нотация](#[rpn](https://github.com/Haise3s/Grokking_algorithms/blob/main/Stack%20and%20queue/RPN.py))

---

## 🔗 PSP - Правильная скобочная последовательность

### 📝 Описание
**Правильная скобочная последовательность (ПСП)** — это строка, состоящая из скобок, в которой каждой открывающей скобке соответствует закрывающая, и пары скобок правильно вложены друг в друга.

### 🎯 Основные правила
- Каждой открывающей скобке `(`, `{`, `[` должна соответствовать закрывающая `)`, `}`, `]`
- Скобки должны закрываться в правильном порядке (принцип LIFO)
- Пустая строка считается правильной последовательностью

### 🔍 Примеры корректных последовательностей
- `()`  
- `{}[]()`  
- `{[()]}`  
- `(([]){})`

### ❌ Примеры некорректных последовательностей
- `)(` — неправильный порядок  
- `([)]` — неправильная вложенность  
- `((()` — не все скобки закрыты  
- `{]` — несоответствующие типы скобок

## 🧮 RPN - Обратная польская нотация

### 📝 Описание
**Обратная польская нотация (ОПН)** — это форма записи математических выражений, в которой операнды расположены перед знаками операций. Также известна как постфиксная нотация.

### ✨ Преимущества ОПН
- Не требует скобок для указания порядка операций
- Легко вычисляется с использованием стека
- Используется в виртуальных машинах и калькуляторах

### 🔢 Примеры записи
| Инфиксная запись | Обратная польская нотация |
|------------------|---------------------------|
| `3 + 4`          | `3 4 +`                   |
| `(1 + 2) * 3`    | `1 2 + 3 *`               |
| `5 * (2 + 3)`    | `5 2 3 + *`               |

### ⚙️ Алгоритм вычисления
1. Инициализируем пустой стек
2. Проходим по каждому элементу выражения:
   - Если элемент — число, помещаем в стек
   - Если элемент — операция:
     - Извлекаем два операнда из стека
     - Выполняем операцию
     - Результат помещаем обратно в стек
3. В стеке остаётся результат вычисления
